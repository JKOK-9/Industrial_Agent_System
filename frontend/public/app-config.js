window.__APP_CONFIG__ = {
  // Backend API address. Keep empty when the frontend and backend share the same origin or when using a reverse proxy.
  apiBaseUrl: "",

  // Micro-frontend metadata for deployment records. The current app does not use vue-router.
  microAppName: "industrial-agent-system",
  baseRoute: "",
};
