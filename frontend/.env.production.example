# Build-time config.
# Use a relative base by default so micro-frontend hosts can load JS/CSS from the child app URL.
VITE_PUBLIC_BASE=./

# Usually leave this empty and edit dist/app-config.js after build.
VITE_API_BASE_URL=
