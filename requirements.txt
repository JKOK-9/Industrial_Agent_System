# Runtime: Python 3.12.x
#
# PyTorch CPU wheels are resolved from the official CPU index below.
# Install with: python -m pip install -r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cpu

# Packaging tooling for fresh Python 3.12 environments.
pip>=24.3
setuptools>=75
wheel>=0.45

# Backend API.
Flask==3.1.0
Flask-Cors==5.0.0
pydantic>=2.12,<3
PyYAML==6.0.2
httpx>=0.28,<1

# Model download and inference.
huggingface-hub>=0.34.0,<1.0
modelscope==1.37.1
numpy==2.3.5
torch==2.11.0+cpu; platform_system != "Darwin"
torchvision==0.26.0+cpu; platform_system != "Darwin"
torchaudio==2.11.0+cpu; platform_system != "Darwin"
torch==2.11.0; platform_system == "Darwin"
torchvision==0.26.0; platform_system == "Darwin"
torchaudio==2.11.0; platform_system == "Darwin"
transformers>=4.55,!=4.57.0,!=4.52.0,<5.0
accelerate>=1.3.0,<2.0
peft>=0.18.0,<0.19
llamafactory==0.9.5
safetensors>=0.4,<1
sentencepiece>=0.2,<1

# Data import and graph storage.
pandas>=2.2,<3
openpyxl>=3.1,<4
neo4j>=5.28,<6
